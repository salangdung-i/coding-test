// you can write to stdout for debugging purposes, e.g.
// console.log('this is a debug message');

function solution(N) {
  // 1. 이진수로 바꾸기 
  let binary = N;
  while (binary < 0) {

    binary % 2 =
  }

  // 2. 0 gap 담기 

}


const N = 1041;
solution(N);
